name: sendit
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^2.5.3
  equatable: ^2.0.2
  firebase_auth: ^3.1.2
  firebase_core: ^1.7.0
  firebase_storage: ^10.0.4
  fl_chart: ^0.36.1
  flutter_ble_lib_ios_15: ^2.5.0
  flutter_riverpod: ^0.14.0+3
  flutter_slidable: ^0.6.0
  geolocator: ^7.2.0+1
  json_annotation: ^4.1.0
  mapbox_gl: ^0.14.0
  path_provider: ^2.0.3
  permission_handler: ^8.0.0+2
  rxdart: ^0.27.0
  sensors_plus: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.1.2
  json_serializable: ^5.0.0

flutter:
  uses-material-design: true

