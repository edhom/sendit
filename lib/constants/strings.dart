/// The title of the application
const String kAppTitle = 'sendIT';

/// The title of the [HomeScreen].
const String kHomeTitle = 'Home';

/// The title of the [HomeScreen].
const String kRecordsTitle = 'Records';

/// The title of the [MapScreen].
const String kMapScreenTitle = 'Trails';

/// The title of the [FindSensorsScreen].
const String kFindSensorsTitle = 'Sensors';

/// The title of the [SettingsScreen].
const String kSettingsTitle = 'Settings';

/// Text showing when no inputs are found.
const String kNoInputsHint = 'No inputs found.';

/// The text of the button to monitor an input.
const String kMonitorButtonLabel = 'Monitor';

/// The text of the button to analyze a record.
const String kAnalyzeButtonLabel = 'Analyze';

/// Hint to show if a ble device doesn't provide any services.
const String kNoServicesFoundHint = 'This device does not provide any services';

/// Hint to show if a ble service doesn't contain any characteristic.
const String kNoCharacteristicFoundHint = 'No characteristics found';

/// Label for the text field to enter the name of a new input.
const String kInputNameLabel = 'Name';

/// Label for the dropdown to select the type of a new input.
const String kInputTypeLabel = 'Type';

/// Label for the toggle button to extract from json.
const String kInputJsonToggleLabel = 'Extract from json';

/// Label for the text field to enter an optional json key.
const String kInputJsonKeyLabel = 'Json key';

/// Label for the text field to enter the min value of a new input.
const String kInputMinLabel = 'min';

/// Label for the text field to enter the max value of a new input.
const String kInputMaxLabel = 'max';
